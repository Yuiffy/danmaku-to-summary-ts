# 自动晚安、图片与切片优化实施记录

目标：落实同日优化审计，保持模型、推理档位、媒体画质、必要证据和上传授权不变。用户先批准三个窗口，随后明确批准 9 月 5 日整场文字的四批对照；32 次请求已完成，但新版整场选材均值慢约 14.5%、输入 tokens 多约 11%，仍未通过验收。详见 [整场真实结果](post-stream-full-stream-ab-results-2026-09-07.md) 及较早的 [局部对照](post-stream-ab-results-2026-09-07.md)，本文不是上线验收通过声明。

2026-09-07 续作：此前被审批上下文容量阻断的剩余文本修复已完成，并激活 `589aec2c1d8b899cd1fc`。Node/Python 都按响应的 `phase` 提取最终答案，修复 Node 非数组 `output` 的兼容异常，保留失败场景的已知用量。此次激活仅涉及已验证的文本适配器及缓存协议，不代表完整选材策略的冷启动与质量验收已经通过。

2026-09-07 最新续作：缓存路由版本 `3fd40b94801fce107220` 已激活。相对上述版本只改变编译后的 `text/requests.js` 及声明文件；未选中显式缓存灰度的共享前缀请求只增加 `prompt_cache_key`，不改变角色、单块正文、模型、推理或输出上限。另完成相邻事件误去重、空证据分块跳过及缓存参数兼容回退修复。完整选材的性能与语义质量仍未验收，不能将本次切换理解为全部增强已经合格。

后续已激活 `ecd0450cbeadd3aa9dc4`：修复失败用量传递、非终态/截断响应及坏缓存复用，保留正常输出与原模型参数。逐片文案试验明显缩短了文案阶段等待，但输入更多且质量非劣化未证明；新的静态前缀路由没有测到新增用户块命中，默认不启用。见 [文案对照与用量修复](post-stream-copy-accounting-2026-09-07.md)。

2026-09-07 媒体验证续作：已用三个授权窗口跑完旧、新、新、旧四批实际压制与封面。对应 MP4、SRT 和封面全部逐字节一致，分辨率探测每批 3 次降为 1 次，GPU 峰值查询平均减少约 38%。媒体阶段均值约 102.4s 对 97.1s，但首批旧版偏慢、样本少，不据此宣称整场提速或 P95 达标。源码基线为 `54e1a38`，此次只写隔离测试产物与记录，没有再次修改生产流程。见 [实际媒体对照](post-stream-media-validation-2026-09-07.md)。

2026-09-07 晚安续作：从生产诊断稿确认中文感叹号未计入句数，导致完整两句被判为一句并多生成一次；已修复中文句界及纯标点尾段计数，字数/至少两句等门槛不降。另补晚安外层对明确进行中响应的停止重提保护，两个 provider 的完整入口模拟由 3 次请求降为 1 次。19 份已保存正文保持原有通过状态和清理结果，当前 80 个 Jest 套件、914 项及 Node 11 项通过。生产中也确认了超过 11 万 tokens 的共享事实块缓存命中，但同时有相同 key 的梗概零命中，不外推整体命中率或耗时收益。见 [晚安重试与生产缓存验证](post-stream-goodnight-validation-2026-09-07.md)。

2026-09-07 整场测试准备：已冻结 `3fd814e`/`650b424` 选材源码及同一份 9 月 5 日整场文字、情绪输入，完成四批共 32 次模拟请求和 17 项保护测试，实际网络请求为 0。新版工具不再用可变 HEAD 作为旧版基线，并预先记录评审维度及计量规则；它没有 live 模式，不将自动继续目标视为新授权。整场素材外发仍等待用户确认，详见 [整场测试离线准备](post-stream-full-stream-test-preparation-2026-09-07.md)。

其后用户明确允许整场测试，新增隔离真实执行入口，严格按旧/新/新/旧完成 32 次 Luna/high 请求，无重试或备用路由。旧/新选材均值 701.555s/803.066s，主要增加在重排（206.229s/334.087s）。新版最终时长均守住当前规则，旧版后对齐产生 15 条超长；但 19 条匿名源文抽查中两版均有事实或时序问题，不宣称整体准确率提高。真实响应的本地阶段缓存回放也未通过，两批只命中 0/1 个阶段，主要因片外弹幕引用，不能计为缓存节省。新实验没有修改生产源码、配置、发布指针或上传状态，详见 [完整记录](post-stream-full-stream-ab-results-2026-09-07.md)。

## 已实现

- 修复 `own_selection.js` 缺少 `buildEmotionContextLines` 导入，新增分块及整条 staged 选材测试。
- 新增 `clipping/subtitle_evidence.js`：连续同说话人短字幕组成稳定 G-ID 小段，完整保存原文和精确时间映射。显示时间使用简洁的秒数，实际裁切从 ID 取原始时间。不会跨明显停顿或不同说话人合并；原本就很长的单条字幕不会被猜测拆分。
- 分块召回不再丢弃尾部字幕，staged 召回只写事件和证据，不提前写一套最终标题、封面和简介。分块数量、并发、模型及推理设置不变。
- 修复分块结果按“相隔不超过12秒”去重而丢失独立事件的问题，改用候选池已有的实质重叠判定。相邻和少量重叠的备选可进入全局编辑，明显重复仍去重，候选上限不变；保留更多候选可能增加重排输入，不把它宣称为 token 优化。
- 仅在一个分块没有字幕、弹幕和情感/声音证据时跳过模型调用，`PLAN.aiStatus.skippedChunks` 保留原因，不计入请求或失败。纯弹幕、纯情感块不跳过。六场历史审计没有这种空块，因此这六场不节省请求次数。
- 首轮给实际展示的弹幕分配稳定 D-ID；首轮和重排共用 ID/时间校验。重排保留被有效首轮定位引用的原始弹幕，即使其常规采样本来会略过这些评论。先写文案再全局排序的实验没有稳定提速，已经撤回，详情见真实测试报告。
- 全局重排读取按候选范围去重的完整字幕表及带 D-ID 的弹幕样本。支持 G-ID 精确起止、关键证据引用和 live_speech/recount/playback/audience/uncertain 来源标记。
- 真实测试后，重排可复用同源、同范围且引用验证通过的首轮定位，并可显式覆盖；首轮自然语言概括不再进入重排，避免“引用误写成现编”等错误逐级传播。文案必须区分转述、亲历、引用/创作与事件先后；没有弹幕引用的观众归属陈述会标待审核。被剔除候选的原因进入诊断，不再静默丢弃。
- 重排输入进一步采用有列定义的紧凑数组、召回理由字典、长弹幕同文引用；原话、每次出现 ID、窗口频率、全部信号和精确时间映射保留。未知来源名称不会被别名覆盖。新 PLAN 明确记录 `complete_grouped_v1` 和 `subtitleTruncation=false`，不再写入未使用的 520 字符截断上限。
- 未知候选 ID、未知或未提供的边界 ID 不会被静默映射为另一个候选。缺少引用、越界引用、无法在所引原文找到的引号内容或多位数字，会标为 `needs_review`。`linked` 只表示出处及时间关联通过，不代表 ASR 或语义已经人工确认。
- 后续补齐严格的时刻表示校验：19:52可关联到已引用、已提供且片内的“十九点五十二分”，避免四个数字表示误报。只确认完整时刻的写法等价，不推断时区、12小时制或任意中文数量，不改文案/字幕或隐藏其他引用警告。16条保存结果的本地重放只改变该条审核标记，新增模型调用为0。
- 新协议的边界已落在完整字幕小段上，不再被后处理任意缩短。旧时间戳计划仍可读取；有证据元数据的计划在最终边界调整后重新检查，源哈希变化会标记 `source_changed`。
- PLAN、逐片 metadata 和 REVIEW 保留证据及待核对原因；没有增加自动上传权限，也没有重写已有投稿资料。
- 后续边界审计补齐新 `PLAN.config` 的 `minClipSeconds` 与 `maxClipSeconds`，保存当次实际限制，避免用后来配置追判旧切片；不改变请求或裁切结果。
- staged 重排补齐已有 `avoidOverlappingClips`/`finalOverlapToleranceSeconds` 配置对应的提示词，提前告知最终互不重叠和最小间隔要求；关闭配置时不添加。不改过滤器、源证据或模型，不新增修复重试。8组真实提示词本地比较只有对应规则增加，单次真实功能验证2条均通过重叠过滤，其中1条的片外引用仍标待审核，不宣称整体文案准确率或速度已验收。
- 真实响应暴露的顶层 JSON 数组现在可以直接解析，与 `clips` 对象包装共用同样的校验，不因包装差异丢弃已经生成的内容。已用原响应本地重放恢复三条切片，没有新增模型调用。
- 新增 `clipping/selection_cache.js`，按实际 prompt、阶段、模型及请求配置缓存有效结果，并合并同进程/跨进程的相同请求。失败、无效结果不落缓存，源内容、配置或 prompt 变化会失效。每场默认保存在切片目录下的 `.selection-cache`；`ownStreamClips.ai.selectionCacheEnabled=false` 可禁用。
- 缓存命中及加入同进程在途请求的消费者，本次模型用量计为 0，原请求用量放入 `reusedGeneration`，不会重复累计。缓存校验与最终选材共用候选边界校验，不能缓存串窗结果；仅在端点接触不算有效内容重叠。引用还必须出现在实际提供给模型的证据表中，未知/越界/未提供引用不进入有效缓存，相关告警不会在后续边界复核时消失。
- 阶段缓存签名加入实际请求协议版本。全量统一角色实验虽减少输入 tokens，却未通过速度验收，已撤回；当时版本 3 用于排除版本 2 实验缓存，本轮因最终正文提取修复升级到版本 5。原有缓存灰度可能影响角色的问题并未宣称解决。
- 晚安生成可写入 `_SHARED_LIVE_SOURCE.json`，漫画复用通过源哈希、前缀哈希及房间校验的同一份事实前缀。晚到的梗概、直播时间和任务约束完整保留在后缀，不再改变前面的源事实。
- Node/Python 统一文本尝试归属：默认单请求上限 120 秒、总预算 240 秒，Python 只给进程退出保留 5 秒余量。Node 内部同模型 provider 容灾保留；已由 Node 管理的失败不会再启动独立 Python 重试链。Node 不可用或在请求开始前启动失败时，原 Python 备用路径仍保留。
- 文本结果带 SHA-256。若输出已完整生成但进程没有及时退出，Python 校验哈希后可复用结果；无法证明完成的超时标记 `outcome_unknown`，不把未知用量当零。
- 完整文本被 Python 脚本校验拒绝时，仍保留 Node 报告的模型用量；进程超时且输出不完整时也保留已知请求记录，不覆盖成无用量的状态。哈希一致不等于内容合格，无效脚本仍被拒绝，未增加 Python 重试链。
- Node自身拒绝空结果、过短及截断结果时也保留已知用量、HTTP状态和请求ID；未报告的部分保持未知。选材诊断记录失败并汇总全部尝试，加入同一失败请求的消费者不重复计费。Python逐次记录Node尝试，不再只打印最后成功一次。
- 明确进行中的响应不作为完成文本或最终用量，不继续切换模型/供应商重复提交。已知未完成阶段结果不落缓存，旧的明确坏缓存选择性失效；漫画脚本元数据损坏、失败或明确截断时不复用，正常结果和已有图片仍保留。
- 新的 Node/Python 提取器在有明确 `commentary` / `final_answer` 标记时优先取最终答案，不把进度说明拼入晚安或漫画正文；最终答案缺失或被拒绝时不回退发布进度。无 phase 的旧接口保持兼容，不按“我会先”等措辞删内容。上游已计费的进度 tokens 仍计入用量。
- Node 对非数组 `output` 同时保护正文提取和结束状态读取。Python 提取函数移入 `comic/text_response.py`，保留原导入名称，未抬高大文件行数预算。
- 漫画脚本缓存策略版本升为 17，旧策略脚本不再复用，当前策略脚本仍可复用；已有成品图片提前返回，不会因策略升级重做。版本升级的首次重算不是 token 节省，后续合法缓存复用行为不变。
- `clipping/video_probe.js` 异步探测分辨率，按路径、探测程序和文件身份/大小/mtime/ctime 复用进行中的请求和成功结果。失败不缓存，文件变化失效，调用者修改返回对象不会污染缓存。
- 并行压制共享同一采样周期的 GPU 查询，仍按原采样频率运行，新加入的短阶段仍有初次采样。不改 NVENC 参数、字幕尺寸、两阶段 copy 压制或游戏负载保护。
- 全局重排的多个候选共用一次弹幕时间索引，保留原始对象、D-ID、同时间顺序和闭区间边界；单候选仍直接筛选。频次统计和样本排序在同一窗口内复用，原文和采样规则不变。索引不跨重排调用缓存，输入变化会重新构建。
- 漫画与后台切片的安全并行支撑已实现：以同盘临时硬链接保留漫画输入，登记读者 PID、故障时保守回收，跨盘/不可链接时退回串行。`ai.comic.overlapClips` 默认关闭，文件与 FFmpeg 检查已通过；真实文本调度虽然缩短总耗时，漫画脚本反而推迟 8.932 秒，未通过不变慢门槛。详见 [安全并行记录](comic-clip-overlap-2026-09-07.md)。

新增代码按职责放入 `selection_request.js`、`selection_result.js`、`video_probe.js`、`text_generation_protocol.js` 和 `comic/text_client.py`，没有抬高大文件行数预算。架构规则只为托管 Node 文本桥接允许 subprocess，仍禁止它直接导入 provider HTTP 实现。

## 离线证据

`tmp/poststream-offline-validation-20260906.cjs` 把所有模型函数替换为本地桩，不外发字幕或弹幕。

- 六场共 20,371 条原始字幕，重建后原文、开始时间和结束时间逐条完全相等。
- 所有字幕进入召回输入，仍为原来的 27 个分块。
- 分块字幕表示从旧版实际保留的 337,994 字符，变为完整的 325,804 字符，约少 3.6%。旧值不包含截断提示标记；字符不是计费 tokens。
- 同一个真实 1920x1080 录像的 31 个并发分辨率调用，只启动了 1 个 ffprobe，最近一次耗时约 98ms。没有解码或重压制整条视频。
- 阶段缓存测试验证：同输入重复运行及同进程并发只生成一次，参数/源/prompt 变化会重新生成，错误不会触发隐式第二次调用。
- 模型结果验证测试覆盖越界、未知/未提供 ID、仅端点接触的串窗、不支持的引号/数字、源字幕变化、正常响应及无害 JSON 包裹文字。
- 重试测试覆盖正常完成、托管错误、不明超时、已完成但迟迟不退出、stdout 哈希错误，以及 Node 不可用时的备用路径。

### 不能忽略的输入增加

只用已选片段构成稀疏候选集时，完整证据仍比旧版每候选 520 字符更长。用本地召回加可恢复的模型候选重建池后，紧凑编码让四场完整证据输入低于旧截断版；两场长直播仍明显增加：

| 日期 | 重建候选数 | 旧截断版字符 | 初版完整证据 | 紧凑完整证据 |
| --- | ---: | ---: | ---: | ---: |
| 08-29 | 88 | 110,529 | 150,740 | 140,595 |
| 08-31 | 81 | 103,891 | 111,445 | 101,074 |
| 09-01 | 62 | 79,355 | 82,122 | 73,887 |
| 09-03 | 46 | 57,400 | 61,776 | 56,525 |
| 09-04 | 82 | 101,028 | 107,165 | 94,939 |
| 09-05 | 90 | 112,891 | 146,393 | 137,492 |

六场重排合计从初版完整证据的 659,641 字符降至 604,512，减少约 8.36%；相对旧截断版合计 565,094，仍增加约 6.98%。这仍只是字符量，不是模型用量或延迟测量。

历史完整首轮模型候选没有全部保存，因此这是明确标注的重建池，不是伪称恢复了原生产请求。不能把分块节省 3.6% 说成整条链路节省 3.6%，也不能用本地构造耗时替代模型延迟。恢复中段事实可能改善文案，但质量和端到端速度仍须真实 A/B。

### 弹幕索引的本地对照

后续检查发现，重排对每个候选重复扫描、排序整场弹幕；同一个窗口还重复建立同文频次表和排序样本。先保存改动前两个模块的原文及哈希，再对修改后的实现做离线深度比较，不调用模型或解码媒体。

六场共 64,211 条弹幕、20,371 条字幕、449 个重建候选和 27 个召回分块。重排返回的完整结构以及召回分块结构均逐项相等，包含全部字幕、弹幕样本、原文、时间、ID、频率、信号及模型定位提示；不会改变本次优化前模型收到的证据或 token 数。

每场先热身四轮，再交错运行旧、新各二十轮。第二次完整复测：

| 录播 | 弹幕数 | 候选数 | 旧准备 P50/P95 | 新准备 P50/P95 |
| --- | ---: | ---: | ---: | ---: |
| 08-29 | 10,501 | 88 | 28.08/30.82ms | 16.05/19.95ms |
| 08-31 | 5,672 | 81 | 15.48/18.11ms | 11.20/12.92ms |
| 09-01 | 3,525 | 62 | 9.84/11.75ms | 7.60/8.71ms |
| 09-03 | 3,032 | 46 | 8.95/11.42ms | 7.07/8.41ms |
| 09-04 | 28,501 | 82 | 56.33/61.23ms | 36.27/41.14ms |
| 09-05 | 12,980 | 90 | 32.89/41.63ms | 21.41/28.32ms |

两轮复测的各场中位耗时下降约 17%-43%。第二轮所有计量重复的进程 CPU 时间合计由 3,238ms 降至 2,091ms，约少 35.4%；这是输入准备部分的 CPU 数据，不是整条生产流水线的节省。索引额外占用 O(N) 的临时空间，没有声称内存同步下降。

新增随机乱序区间、同时间稳定顺序、重复对象、闭区间端点、非法/无限时间、采样数量和下一轮源变更测试。单候选不建立全场索引。原先完整证据冷启动的模型耗时门槛仍未通过，不用这些毫秒级收益抵消该问题，也没有再为本地等价改造消耗远端 tokens。

脚本：`tmp/benchmark-danmaku-evidence-index-20260907.cjs`。冻结基线：`tmp/danmaku-evidence-before-index-20260907.json`。两轮结果：`tmp/danmaku-evidence-index-2026-09-06T19-57-31-673Z.json`、`tmp/danmaku-evidence-index-2026-09-06T20-04-25-818Z.json`。候选来自明确标注的重建池，不冒充历史完整模型候选。

## 远端缓存情况

后续成功验证：采用兼容的隐式路由、人工固定文本，两次相同前缀不同后缀的请求均成功；第二次 cached tokens 为 2,816/3,685，其中输入事实块命中 2,784，instructions 命中 32。未缓存输入降为 869，耗时从 1.574s 到 1.092s。该结果只证明当前网关可以命中，不能外推每场命中率或费用。以下为此前失败探测的历史记录。

人工固定文本探测得到：当前 daiYu 网关的 `gpt-5.6-luna` 明确拒绝 `prompt_cache_breakpoint`；另一些请求返回上游 502，未获得成功用量。

按实际兼容性修正 Responses 请求：保留稳定 prefix、路由 key 和用量记录，但不在缺少断点时指定 `explicit` 模式。Chat Completions 的已有显式兼容路径不改变。可观测状态改为 `implicit_routed`，它不是“缓存命中”的声明，仍看返回的 cached tokens。

此前失败探测没有可靠 token 用量，不能声称零消耗；后续成功验证已补上可观测用量，但仍不作生产全量命中率或计费节省承诺。

另一个审计补充：虽然 Python 的 `outputLockEnabled=false`，正常自动漫画入口的 JS 包装层已经调用 `acquireComicOutputLock`。因此没有再盲目打开同名 Python 锁，以免父子进程互相等待。

### 不改变输入的缓存路由

最新真实适配器测试使用人工文本和不同任务后缀，禁止在测试代理中修改请求体。两次都是原来的单块 user 正文、未指定 instructions、Luna/high、100,000 输出上限。输入均为 8,698 tokens，输出均为 8 tokens；缓存命中从 3,840 增至 7,936，其中第二次有 3,556 个输入事实 tokens 命中，不只是固定指令。

未缓存输入从 4,858 降到 762，约少 84.3%；耗时为 4.669s、1.788s。这只证明当前网关支持此路由，既不是总输入 tokens 减少，也不是生产命中率、费用或端到端耗时保证。之前的双块实验已撤回，最终方案只有路由键变化。阶段结果缓存和漫画脚本策略版本仍为 5 和 17，不为路由变动重做已有合法结果。

上游若以 400/422 明确拒绝缓存字段，移除可选提示后在同一 API、同一剩余截止时间内重试；保留原角色、消息和媒体字段。无关错误不会被当作缓存错误或重复读取响应正文。被拒绝请求的用量未知会单独记录，不当作零成本。当前 Responses 网关已真实验证接受该路由。

此路由只适用于带共享事实前缀的请求；当前 staged 召回和重排不因此自动获得共享前缀，不用本测试解释它们的冷启动速度。

结果：`tmp/implicit-prefix-adapter-63edd4c5-1205-4f9b-a9c2-63f200ef24a1.json`。离线发布核对覆盖 Responses/Chat、high/max 和四种缓存状态，共16组请求体比较：除符合条件的可选路由键外，全部字段相同。编译产物新增单块输入回归测试。

### 分块边界尚待修复

六场137条历史切片中，91条处于当前35-215秒验收范围，89条的全部字幕能出现在至少一个现有分块中；08-29第29条与09-05第27条跨分块，不能满足这一条件。其余46条超出当前时长范围，历史 PLAN 缺少当时的 min/max 配置，不能据此认定它们违法或误切。

离线设想在分块尾部补最多215秒上下文，能覆盖91/91条，但字幕表示由325,804增到349,469字符，增加7.26%，尚未计入弹幕与模型耗时。没有修改生产分块，也没有上线此方案。需要同时校验核心区起点归属、跨边界字幕、尾部弹幕 ID 和缓存边界，不能仅扩大文本范围。

第27条的局部上下文缺少准备过程和考试使用的收尾。补测范围为09-05录播04:28:00-04:31:15，仅取完整落在窗口内的44条字幕和103条弹幕；相对该条原授权范围增加30条字幕和73条弹幕。用户随后明确回复“允许”，该窗口已完成真实对照；其他外发范围没有扩大。原离线审计见 `tmp/poststream-chunk-boundary-audit-20260907.json`。

后续已在 `tmp/poststream-boundary-candidate-20260907/` 完成隔离候选实现，而非只计算假设窗口：12项本地测试和六场原始证据保留检查通过，完整字幕覆盖仍为89/91到91/91，原有27次召回不增加。按照 staged 首轮实际构造的提示词总计从935,192增到987,468字符，约增5.59%；这是字符量和本地构造验证，不是 token、标题准确率或冷启动非劣化证明。生产分块没有改动，详见 [候选实现报告](post-stream-boundary-candidate-2026-09-07.md)。

获授权后完成当前版/候选版/候选版/当前版四轮真实staged对照，共12次同模型high请求。两阶段均值120.310秒对163.609秒，候选慢约36.0%，输入增加约3.22%、输出增加约34.40%；候选只有一轮补齐预定义完整事件，另一次仍漏收尾且有证据不足的归属断言。因此不启用尾部扩展。进一步复核发现，四轮重排其实都能看到全部目标原话，但候选池合并时较宽的本地范围被较窄模型边界覆盖，可能使完整故事无法通过边界校验；此处是后续要解决的具体问题，不属于ASR模型升级。

随后完成 `contextWindow` 隔离候选，将建议裁切和证据范围分开保存。12项离线测试、六场449个重建候选审计通过，原候选数量、评分、分块及原证据保留；重排字符量增加4.32%。复用已成功首轮响应、只新增4次重排的匹配对照中，旧均值98.428秒、候选102.674秒，质量和完整性仍不稳定，未复制到生产。另落地上述已有不重叠配置的提示词一致性修复。详情见 [范围保留与重叠规则报告](post-stream-context-preservation-2026-09-07.md)。

## 验收状态

最新续作回归：Jest 75个测试套件、852项、`type-check`、`build:check`、独立Node 11项通过；漫画相关Python 86项、Python文本协议13项通过。真实Node子进程的离线失败回放确认已知用量传到Python且不额外调用模型。已切换到上述文本完成状态与诊断修复版本，未开启更宽范围、逐片文案或新的静态前缀路由；全仓架构检查仍有两处其他任务改动的行数预算问题。

- [x] 分块运行回归及 staged 路径测试。
- [x] 字幕原文、精确时间和召回覆盖的离线一致性验证。
- [x] 阶段缓存、源变更失效、并发请求及失败行为测试。
- [x] JS/Python 前缀一致性、晚到上下文、损坏/过期 sidecar 测试。
- [x] 托管重试、结果哈希和不明完成状态测试。
- [x] phase 正文提取、非数组响应兼容、失败用量保留及旧脚本/成品图片缓存边界测试。
- [x] 异步探测和共享采样测试；真实视频头信息复用验证。
- [x] 三个授权窗口的四批真实媒体对照：MP4/SRT/封面字节一致，画面、音轨、时间轴和字幕显示检查，以及相同调度档位下的探测/采样次数与媒体阶段耗时记录；不替代完整流水线验收。
- [x] 六场弹幕时间索引与原实现的全部证据/分块深度比较，两轮交错本地耗时和 CPU 验证。
- [x] TypeScript 类型检查与构建检查；独立 Node 发布包启动测试。
- [x] 用户明确授权的三个实际片段已完成重排及两阶段测试，结果和失败实验保留。
- [x] 后续授权的 9 月 5 日整场四批、32 次同模型同推理请求已完成；保留原始请求/响应、提议/接受/最终数量、耗时、用量、19 条源文抽查与两批失败缓存回放。不将测试执行完成标为性能或质量门槛通过。
- [x] 固定相同提示词的四次请求角色对照完成，未达标实验撤回；实际 JSON 数组响应本地重放和双包装校验测试通过。
- [ ] 真实素材的旧版/新版标题、简介及起承转合盲评。
- [ ] 模型用量、晚安/图片/首片/全部切片完成时间及 P50/P95 的匹配对照。
- [x] 当前供应商成功请求的缓存命中验证（人工固定文本）。
- [x] 本轮文本适配器修复的版本切换和实际运行回放。
- [x] 仅添加缓存路由键的真实适配器验证、同 API 兼容回退与编译包回归。
- [x] 相邻独立事件、空分块诊断及零额外请求的阶段缓存重放测试。
- [ ] 完整自动选材增强的最终性能与质量验收。

真实素材 A/B 最初被安全审批拒绝；用户随后明确回复“同意，可以测试”，获批后才执行。仅选中三个实际片段的字幕、弹幕和提示词经 daiYu 网关转发上游；本批真实请求未触发 tuZi 备用路由，没有发送视频或发布内容。

当前运行版本为 `build/workflow-releases/ecd0450cbeadd3aa9dc4`，本次从 `3fd40b94801fce107220` 切换，清单只改变 `workflows/text/response.js` 和对应声明文件。24份已保存成功响应的正文、结束状态和用量提取一致；6组当前默认选材请求与静态前缀试验前的prompt和模型选项相同，未开启新路由。通过现有构建工具原子激活后，两个CLI检查和Node→Python故障回放通过，没有重启服务或中断在途任务。先前各版本保留为冻结产物，已有进程保留已加载模块。

上述 source CLI 修改随后已按用户要求全量提交并推送为 `35394a8`，人物引用检查 `08c06d5` 与跨进程失败共享 `54e1a38` 也已推送。最近一次全仓回归为 79 个 Jest 套件、885 项及独立 Node 11 项通过，类型与构建检查通过。提交、文本适配器激活及本次媒体等价验证仍不等于完整自动选材的质量和冷启动性能验收。

当前全仓架构检查为 `audio_processor.js` 1622/1496、`sensevoice_speaker.py` 2388/2331；早先的 `asr_backends.js` 超限已被其他任务改动消除。本任务没有修改这两处其他任务代码或调整预算。

## 复核文件

- `tmp/poststream-offline-validation-20260906.json`：完整性、各阶段输入大小及探测复用统计。
- `tmp/poststream-offline-validation-20260906-compact-rerank.json`：紧凑重排的离线对照统计；模型函数均为桩，没有真实素材外发。
- `tmp/poststream-cache-probe-20260906.json`、`tmp/poststream-cache-probe-20260906-implicit.json`：人工文本探测状态。
- `tmp/poststream-selection-ab-20260906.cjs`、`tmp/poststream-staged-ab-20260907.cjs`：已授权的三片段对照工具，脚本限制样本范围；历史实验及计量见真实测试报告。
- `docs/post-stream-optimization-audit-2026-09-06.md`：修改前调查记录，保留历史事实；本文是后续实施及验收状态。
