# 自定义 AI Prompts 功能说明

## 概述

现在系统支持为特定主播配置专用的 AI 生成 prompts，包括：
1. **晚安回复 prompt** (`goodnightReply`)
2. **漫画脚本生成 prompt** (`comicScript`)
3. **漫画图片生成 prompt** (`comicImage`)

## 配置方式

在 `config/production.json` 的 `ai.roomSettings` 中，为特定房间添加 `customPrompts` 字段：

```json
{
  "ai": {
    "roomSettings": {
      "612978": {
        "audioOnly": true,
        "characterDescription": "吃蛋挞的折棒，黑色长发、紫红瞳的喜欢三国杀的女生；喜欢打扮成三国人物的样子。",
        "anchorName": "折棒爷",
        "fanName": "折棒棒",
        "enableTextGeneration": true,
        "enableComicGeneration": true,
        "enableDelayedReply": false,
        "customPrompts": {
          "goodnightReply": "你的自定义晚安回复 prompt...",
          "comicScript": "你的自定义漫画脚本生成 prompt...",
          "comicImage": "你的自定义漫画图片生成 prompt..."
        }
      }
    }
  }
}
```

## Prompt 模板变量

### 晚安回复 (goodnightReply)

可用变量：
- `{anchor}` - 主播名称
- `{fan}` - 粉丝名称
- `{wordLimit}` - 字数限制
- `{highlightContent}` - 直播内容摘要

### 漫画脚本生成 (comicScript)

可用变量：
- `{character_desc}` - 角色描述
- `{highlight_content}` - 直播内容摘要

### 漫画图片生成 (comicImage)

可用变量：
- `{character_desc}` - 角色描述
- `{comic_content}` - 生成的漫画脚本内容

## 示例：折棒爷的三国杀主题配置

已为房间 `612978`（折棒爷）配置了三国杀主题的自定义 prompts：

### 晚安回复特点
- 强调三国杀游戏元素（武将、技能、操作）
- 包含吃蛋挞、cosplay 三国人物等特色内容
- 使用三国杀术语和梗

### 漫画脚本特点
- 如果直播涉及三国杀，会在分镜中体现武将形象、技能效果
- 如果涉及 cosplay，会突出服装和造型特点

### 漫画图片特点
- 强调黑色长发、紫红瞳的角色特征
- 可融入三国杀元素（武将形象、卡牌、技能特效）
- 突出 cosplay 服装细节

## 工作原理

1. **优先级**：自定义 prompt > 默认 prompt
2. **JavaScript 文本生成器** (`ai_text_generator.js`)：
   - `buildPrompt()` 函数会检查房间配置中的 `customPrompts.goodnightReply`
   - 如果存在，使用自定义 prompt 并替换变量
   - 否则使用默认模板

3. **Python 漫画生成器** (`ai_comic_generator.py`)：
   - `build_comic_generation_prompt()` 检查 `customPrompts.comicScript`
   - `build_comic_prompt()` 检查 `customPrompts.comicImage`
   - 支持自定义模板的变量替换

## 使用建议

1. **保持一致性**：确保三个 prompt 的风格和主题一致
2. **测试验证**：添加自定义 prompt 后，测试生成效果
3. **变量使用**：确保在自定义 prompt 中正确使用变量占位符
4. **备份配置**：修改前备份原配置文件

## 扩展其他主播

要为其他主播添加自定义 prompts，只需在对应的房间配置中添加 `customPrompts` 字段即可。例如：

```json
"25788785": {
  "anchorName": "小岁",
  "fanName": "饼干岁",
  "customPrompts": {
    "goodnightReply": "小岁专属的晚安回复 prompt...",
    "comicScript": "小岁专属的漫画脚本 prompt...",
    "comicImage": "小岁专属的图片生成 prompt..."
  }
}
```

## 暖心私信写作 Prompt（人工复用）

这份模板用于暖心投稿、感谢信和可能被直播朗读的私信，不是直播摘要、切片文案或自动晚安评论的替代配置。默认只产出草稿，不发送；确认称呼、事实和公开范围后再发布。以下输入标签供人工填写，不是系统现有的模板变量。

### 使用原则

- 先让收件人被准确理解，再考虑展示记忆力和制造笑点。扩大检索范围不等于扩大正文的事件数量。
- 旧 AI 回复和摘要只提供检索线索，不能独立证明事实、说话人、身体状态或发送次数。多份文案重复同一句，也不算交叉验证。
- 分开核对“发生了什么”“谁说的”“谁做的”“为什么”。说话人未确认时，可以保留事件而不指定行动者；连事件也不稳就删除。
- 暖心不是替对方评估缺点后宣布宽容。优先说明这些日子给写信人带来的具体意义，不替全体粉丝发言。
- 公开朗读只听一遍也应成立；一句话被单独摘出时，不应从关心变成催播、查考勤或指责。

### 可直接使用的模板

```text
请为下面的收件人写一封自然、简短、可以被直播朗读的暖心私信。
目标是让对方感到“你记得的是我，也理解这件事对我的意义”，不是展示数据库规模、总结成长履历或要求对方被感动。

【输入】
收件人和正确称呼：
写信账号、身份及与收件人的关系：
场景：私下阅读 / 公开朗读 / 比赛投稿
这次最想传达的一句话：
长度要求：未指定时正文约 150-220 字，宁可更短，不为凑数添加事实
希望保留的口吻或梗：
已知偏好、禁忌及本人公开表达过的想法：
证据材料：每条尽量列明原话或事件、来源、日期或时间点、行动者、是否确认
旧文案：仅供寻找线索与避免重复，不默认准确
是否可公布 ID，以及必须附带的备注：未明确时不要自行授权

【写作顺序】
1. 从材料里选一个情感中心，再选一两件能支撑它的具体小事。
   可以跨很长时间检索，但不要把所有年份、歌名和大活动都写进去。
   宁用一个准确且对方能认出的日常瞬间，不用多个含糊的大事。
2. 核实最终选中的事实，不把旧 AI 文案当成亲历记录。
   直播间里出现某句话，不等于主播说了它；主播说到某件事，也不等于是她做的。
   联动队友、外放视频、游戏角色、弹幕和主播必须区分。
   不确定时缩小表述范围或删除，不添加“认真、带领、累坏、哭了”等没有依据的解释。
   “提到累”不能升级成“累到脚软”；文件数不能升级成成功发送次数或陪伴天数。
   缺乏依据时不要强行让用户提供整场 speaker 识别，优先不用该细节。
3. 用“具体小事 + 我珍惜它的原因”建立温度，少写泛泛的成就和品质评语。
   写信人的感受可以直接表达，但不要杜撰重大人生经历、虚构现场参与，或冒充所有粉丝的共同意见。
   机器人身份如需说明，开头一句足够。不要继续堆硬盘、心跳模块、算法、报错等设定。
4. 不先替对方诊断疲惫、焦虑或不够努力，再宣布“没关系、我允许”。
   除非有明确语境，不把唱错、迟到、忘开麦等列成一串等待被原谅的缺点。
   可以表达喜欢平常的相处，也可以关心休息；不对播多久、几点睡、明天是否开播提出隐含要求。
5. 幽默最多一个，不是必选项。优先调侃自己或双方确认过的小事。
   不用身体、工作态度、早退或不兑现承诺来换笑点；不为收尾硬接包袱。
   用户允许“去掉或改改”时，允许直接删除，不默认必须再造一句笑话。
6. 用短句和明确指代，按对方会读出声的方式重读整封信。
   检查“早、晚、多、少、还、又、总是”等词有没有明确比较对象。
   例如“你说要下播时还早”不清楚是“时间还早”还是“离真正结束还久”，不能使用。
   即使补成了明确的下播玩笑，也要检查它是否仍暗示对方说话不算数；不服务于本次关心就删。
   检查“虽然、但是、不过”之后有没有推翻前一句的关心。
7. 最后一句单独读，应当是支持、感谢或不附带任务的祝愿，不是催促、考核或新梗。
   不必须用“早点休息”收尾，也不承诺永远陪伴或要求对方回复。

【交稿前检查】
- 每个带姓名的动作是否真能归给这个人？每个程度词是否有依据？
- 删除姓名后，是否几乎能投给任何人？若是，用已核实的小事替换套话，不靠加长补救。
- 是否存在“我都付出了这么多，所以你应当……”的隐含人情账？
- 对方是否需要反驳某句、解释自己并不懒、或靠读者善意补全才会觉得暖？
- 结尾是否仍然支持对方，而没有变成对播出时间或工作表现的评价？
- 每次局部修改后，都重新检查整封信，不只看被替换的那一句。

【输出】
先给一版可直接阅读的正文，不带写作说明；公开 ID 等备注按明确要求单独列出。
正文之后另给不超过三条审核说明：采用的事实依据、删去的高风险细节、潜在歧义。
这些说明不属于私信正文。只有明确要求“只给正文”时才省略。
若缺少可核实材料，不编造细节：给出不越过现有证据的短稿，并说明缺少什么。
不要发送、修改线上稿件或自动加入生产配置。
```

### 人工验收例

| 输入或草稿问题 | 应有处理 |
| --- | --- |
| 只有旧回复声称主播讲解游戏机制 | 不把讲解归给主播；核对选中片段或不用该细节 |
| 有 202 个生成回复文件，没有发送结果 | 不写已送达 202 次、不写陪伴了 202 天 |
| “累了就早点下播，虽然你说下播时通常还早” | 删除转折和歧义，检查剩下的关心是否仍在要求作息 |
| “唱错迟到也没关系，你不厉害也值得喜欢” | 不列缺点后给予宽容，转向自己珍惜的已确认相处细节 |
| 用户要求自然一点，允许删掉结尾 | 可以直接删，不把机器设定换成另一种抒情套话 |
| 证据充分但正文像事件清单 | 保留最能承载本次心意的一两件事，说明意义而非堆数量 |

以上是写作约束和人工检查例，不是正确率保证。正式自动化前，需要用不同主播和不同场景的留出样本做对照，分别评价事实准确性、单次朗读理解和人物关系分寸。
